'''To comment in python scripts, surround text with three apostrophes, this
is called a docstring'''

print "Hello world!"

# or use a pound sign to comment
#// don't use slashes, won't work

for i in range(3):
    print i


a = input('Enter integer 1: ')
b = input('Enter integer 2: ')
c = a**2+b**2

print "Hypotenuse squared: " + str(c)

# Don't copy in-shell commands as the code you upload.
# Instead, upload a python script

# Don't copy input into your script
# Instead, read in files

# go over INI6.py and sys.argv
